---
status: entwurf
stand: 2026-08-19
---

# Workflow für Dokumente, Inputs und Kundeninteraktionen

Notiert am 19.08.2026. Noch nicht ausgearbeitet, Ort im Repository noch nicht
entschieden — deshalb hier.

## Was fehlt

Beschrieben ist bisher nur der Weg eines ausgefüllten Steckbriefs: Vorlage
heraus, das Fach füllt, Rücklauf auslesen, Beleg ins Archiv (`CLAUDE.md`).
Nicht beschrieben ist der Rest — welche Dokumentarten es gibt und wohin sie
gehen, wie Input aus dem Fach außerhalb einer Vorlage aufgenommen wird
(Transkript, Mail, Zuruf), und wie Kundeninteraktionen festgehalten werden
(Termin, Rückfrage, Freigabe).

## Was schon verstreut existiert

- `CLAUDE.md`: der Weg des ausgefüllten Steckbriefs; Word, PDF und Außensicht
  sind Ausgabe, nie Eingabe; die Datei im Repository ist der Datenstand
- `4_workspace/README.md` und `5_archiv/README.md`: wo der Rücklauf liegt,
  solange er ausgelesen wird, und wo er danach als Beleg liegt

Ein Workflow-Dokument verweist darauf, statt es zu wiederholen.

## Zu entscheiden

Der Ort. „Wie kommt etwas ins Repository" ist eine Repository-Konvention und
gehört nach `0_meta/`. „Wie läuft die Interaktion mit dem Kunden" ist ein Ablauf
wie Konfiguration oder Lernschleife und gehört nach `2_detailkonzepte/ablauf/`.
Vermutlich werden daraus zwei Dateien, nicht eine.
